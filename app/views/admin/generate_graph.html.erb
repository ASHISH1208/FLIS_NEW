<body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <nav>
          <ul class="nav navbar-nav navbar-right">

            <li><%= link_to "Home",{:controller => 'welcome', :action => 'index'}%></li>
            <li><%= link_to 'Log out', {:controller => 'admin', :action => 'logout'}, :method => 'post' %></li>
            <li><%= session[:a_user_id]%></li>
          </ul>
        </nav>
      </div>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
<center>
<h1>Survey of Loss in Flood</h1><h3><%= session[:a_user_id]%> <h3><br/>
<%= column_chart UserRequest.where("status=1").group(:village).sum(:loss_amount) %>
