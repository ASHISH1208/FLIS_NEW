<body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <nav>
          <ul class="nav navbar-nav navbar-right">

            <li><%= link_to "Home",{:controller => 'welcome', :action => 'index'}%></li>
            <li><%= link_to 'Log out', {:controller => 'admin', :action => 'logout'}, :method => 'post' %></li>
            <li><%= session[:a_user_id]%></li>
          </ul>
        </nav>
      </div>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
<center>
	
<h1>Generate List</h1>
<% if (flash[:msg]) %>
<h2> <%= flash[:msg] %> </h2>
 <% end %>
<%= form_tag("generate_list", method: "post") do %>
  <%= label_tag(:q, "for village:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>

</center>
